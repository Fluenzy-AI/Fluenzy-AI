# Company Tracks Backend Dependencies - Optimized for Render Free Tier
# CPU-only, headless environment

# FastAPI & Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Computer Vision - HEADLESS only (no GUI libraries like Qt, X11)
opencv-python-headless==4.9.0.80

# NumPy - Required for OpenCV and data processing
numpy==1.26.3

# YOLOv8 (Ultralytics) - Will use system torch
ultralytics==8.1.0

# MediaPipe - CPU-only
mediapipe==0.10.9

# Audio processing (lightweight)
sounddevice==0.4.6

# Data processing
scipy==1.12.0
pandas==2.2.0

# Pydantic
pydantic==2.5.3

# HTTP client
aiohttp==3.9.1
